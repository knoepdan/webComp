{
  "name": "web_comp",
  "version": "1.0.0",
  "description": "Testing web components",
  "author": "Dan",
  "engines": {
    "node": ">=16",
    "npm": ">=8"
  },
  "scripts": {
    "dev": "vite --base /",
    "buildAzure": "tsc && vite build --base / && xcopy .\\dist distAzure /s /i /y",
    "build": "tsc && vite build --base /WebComp",
    "copyToLocal": "xcopy .\\dist distLocal /s /i /y",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "vite": "^6.0.5"
  }
}
